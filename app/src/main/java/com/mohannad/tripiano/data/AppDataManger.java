package com.mohannad.tripiano.data;

import com.mohannad.tripiano.data.model.RequestCallback;

public class AppDataManger implements DataManger {
    @Override
    public void login(String email, String password, RequestCallback requestCallback) {
        login(email, password, requestCallback);
    }
}
